import random
while(1):
    temp=random.randint(1,100)
    humidity=random.randint(1,100)
    if (temp>=38):
        if (humidity<=30):
            print("temperature rise")
        else:
            print("humidity is normal while temperature is hot")
    else:
        print("temperature is normal")
